#ifndef INPUTGATE
#define INPUTGATE
#include "Gate.hpp"
#include <vector>
#include <iostream>

using namespace std;
class InputGate : public Gate
{
private:
    /* data */
public:
    InputGate(const string);
    virtual ~InputGate();
};








#endif
